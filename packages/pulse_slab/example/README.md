# Pulse Slab Dart core example

Run this example from the `packages/pulse_slab` directory:

```sh
dart run example/pulse_slab_core_example.dart
```

It defines a compact record layout, allocates one record, commits a
transactional update, and reads the latest committed value without requiring
Flutter.
