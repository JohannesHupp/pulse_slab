name: pulse_slab_flutter_example
description: A focused Flutter telemetry simulation for the pulse_slab_flutter adapter.
publish_to: none
version: 0.2.0-beta.2
resolution: workspace

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  pulse_slab_flutter:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
